function displayElem (elem, destination) {
  //appends the element to the destination on the page
  //maybe has some additional logic
  //if it gets too complicated, break it down into separate functions
};

export default displayElem;